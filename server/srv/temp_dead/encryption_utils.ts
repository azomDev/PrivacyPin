// import * as crypto from "crypto";

// export class EncryptionUtils {
//     static createChallenge(public_encryption_key: string, nonce: string): string {
//         // encrypt the nonce with the key
//         return nonce;
//     }

//     static isSignatureMatching(signature: string, data: string, public_signing_key: string): boolean {
//         return true;
//     }

//     static generateNonce(): string {
//         let nonce_length = 64;
//         return crypto.randomBytes(nonce_length).toString("base64");
//     }
// }
