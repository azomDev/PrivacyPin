{
	"name": "privacypin",
	"version": "1.0.0",
	"type": "module",
	"private": true,
	"workspaces": [
		"server",
		"shared",
		"privacypin"
	],
	"scripts": {
		"vite": "bun run --cwd ./privacypin vite",
		"app": "bun run --cwd ./privacypin tauri dev",
		"nvidia": "WEBKIT_DISABLE_DMABUF_RENDERER=1 bun run --cwd ./privacypin tauri dev",
		"server": "bun run server/src/server.ts"
	}
}
